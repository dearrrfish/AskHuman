### ✨ Features

- **windows**: Add first-class Windows support (named-pipe daemon, tray/GUI host, Terminal tasks, install PATH, and unsigned-safe manual updates via `update prepare`).
- **agents**: Add first-class Pi integration (CLI, managed Extension, lifecycle, and tasks; Pi >= 0.82.0).
- **agents**: Bind lifecycle tracking to automatic integrations; it is on by default and can be turned off per Agent.

### 🐞 Fixes

- **settings**: Open Settings without waiting for Agent CLI version checks.
- **dingtalk**: Reconnect stale Stream sockets.

**Full Changelog**: https://github.com/Naituw/AskHuman/compare/v0.12.2...v0.13.0
